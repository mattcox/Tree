# ``Tree/Node/remove(childAtIndex:)``

```swift
// Remove the child node from it's parent, reparenting its child nodes.
let A = root.remove(childAtIndex: "A")
```

![A tree with a removed node](nodeRemoveRoot.png)

